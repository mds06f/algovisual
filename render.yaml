services:
  - type: web
    name: algovisual
    env: node
    plan: free
    branch: development
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
